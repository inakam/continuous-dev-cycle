{
  "description": "Continuous Development Cycle plugin hooks for auto-commit, expansion proposal, and loop continuation",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.sh"
          }
        ]
      }
    ]
  }
}
